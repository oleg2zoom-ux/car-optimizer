# תיקיית נתונים

`sample_cars.csv` הוא קובץ הדגמה שמאפשר לאפליקציה לעבוד מיד.

`market_samples_template.csv` הוא קובץ תבנית להזנת נתוני שוק אמיתיים.

כל שינוי במבנה העמודות צריך להיות מתואם עם `docs/DATA_SCHEMA.md` ועם הקוד ב-`car_optimizer/data_loader.py`.
